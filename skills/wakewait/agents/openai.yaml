interface:
  display_name: "WakeWait"
  short_description: "Default local sleep and wait rules"
  default_prompt: "Use $wakewait first for sleep, wait, pause, polling, training/download waits, wall-clock state, and deterministic WakeWait rules."
