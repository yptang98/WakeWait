interface:
  display_name: "WakeWait"
  short_description: "Low-token shell sleep and wait"
  default_prompt: "Use $wakewait to wait with bundled shell scripts for sleep, wait-file, wait-contains, or wait-command without model polling."
