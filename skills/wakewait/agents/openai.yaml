interface:
  display_name: "WakeWait"
  short_description: "Deterministic rule waits"
  default_prompt: "Use $wakewait for bundled shell-script file, text, or command readiness waits without model polling."
