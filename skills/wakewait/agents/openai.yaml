interface:
  display_name: "WakeWait"
  short_description: "Low-token wait/sleep routing"
  default_prompt: "Use $wakewait for quiet low-token waits: one shell/session, stdin/status polling if available, no chat progress."
