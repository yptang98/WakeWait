interface:
  display_name: "WakeWait"
  short_description: "Efficient native shell sleep"
  default_prompt: "Use $wakewait to wait with native shell sleep, print the wake time, and avoid model calls while idle."
