interface:
  display_name: "WakeWait"
  short_description: "Native shell sleep and rule waits"
  default_prompt: "Use $wakewait for native shell sleep or bundled shell-script rule waits without model polling."
