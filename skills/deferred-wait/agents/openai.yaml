interface:
  display_name: "Deferred Wait"
  short_description: "Pause or poll long work and resume later"
  default_prompt: "Use $deferred-wait to decide whether to sleep or wait for a condition while a training, download, queue, or long shell task runs."
