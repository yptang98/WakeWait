interface:
  display_name: "Deferred Wait"
  short_description: "Pause or poll long work with fixed rules"
  default_prompt: "Use $deferred-wait to sleep or wait with deterministic WakeWait rules while a training, download, queue, or long shell task runs."
